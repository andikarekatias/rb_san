<%= form_with(model: [notice, notice.comments.build], class: "w3-margin-top") do |f| %>
    <%= f.rich_text_area :body, class: "w3-section" %>
    <%= f.submit "Reply", class: "w3-button w3-blue" %>
<% end %>