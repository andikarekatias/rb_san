<%= form_with(model: pengumuman) do |form| %>
  <% if pengumuman.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pengumuman.errors.count, "error") %> prohibited this pengumuman from being saved:</h2>

      <ul>
        <% pengumuman.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
